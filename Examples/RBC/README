This directory has files for ML and Bayesian estimation of simple RBC model.

The data are 400 observations on C, generated by running rbc.mod, then saving the variable
using "save -ascii c.m c" and then editing by hand to give the final format.

To clean the directory, execute 'sh cleanup' 
